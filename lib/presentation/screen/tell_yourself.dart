import 'package:flutter/material.dart';

class TellYourself extends StatelessWidget {
  const TellYourself({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("Tell Yourself"),
    ));
  }
}
