import 'package:flutter/material.dart';

class AilmentInfo extends StatelessWidget {
  const AilmentInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("Ailemnt Info"),
    ));
  }
}
