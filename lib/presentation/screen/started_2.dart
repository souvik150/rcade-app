import 'package:flutter/material.dart';

class StartedTwo extends StatelessWidget {
  const StartedTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("Started 2"),
    ));
  }
}
